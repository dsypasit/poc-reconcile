apiVersion: 1

providers:
  - name: kvrocks-benchmark-v1
    orgId: 1
    folder: Benchmarks
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards-json
