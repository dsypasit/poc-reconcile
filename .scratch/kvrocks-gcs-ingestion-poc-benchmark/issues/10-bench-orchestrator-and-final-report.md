# 10 - Deliver one-command matrix orchestrator and comparative report

Category: enhancement
Status: ready-for-agent

## Parent

- [PRD](/Users/prasit.sri/Coding/new-cda/plan/.scratch/kvrocks-gcs-ingestion-poc-benchmark/PRD.md)

## What to build

Deliver a single command orchestrator that executes the approved benchmark matrix and generates consolidated JSON, CSV, and markdown comparison outputs from real run artifacts.

## Acceptance criteria

- [ ] One command runs the required matrix for all approaches and required tiers.
- [ ] Consolidated JSON and CSV outputs are generated from run artifacts.
- [ ] A comparative markdown report is generated, ranking approaches with reproducibility context.

## Blocked by

- [09 - Extend all four approaches to medium tier and collect artifacts](/Users/prasit.sri/Coding/new-cda/plan/.scratch/kvrocks-gcs-ingestion-poc-benchmark/issues/09-medium-tier-all-approaches.md)

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Advance this approved vertical slice to implementation-ready state for the Kvrocks ingestion POC benchmark suite.

**Current behavior:**
This issue is specified at triage level with scope, acceptance criteria, and dependencies, but was still in `needs-triage` state.

**Desired behavior:**
The issue is explicitly marked `ready-for-agent` with category `enhancement`, and can be picked up by an AFK agent to implement exactly the slice scope and pass listed acceptance criteria.

**Key interfaces:**
- Benchmark run configuration contract — must support parameters needed by this slice.
- Artifact contract — must preserve JSON/CSV outputs used for comparison.
- Strategy lifecycle contract — ingest, validation, cutover, cleanup phases as applicable to this slice.

**Acceptance criteria:**
- [ ] The slice is implemented end-to-end per "What to build".
- [ ] All acceptance criteria in this issue are satisfied with verifiable outputs.
- [ ] Existing benchmark comparability constraints are preserved.

**Out of scope:**
- Changing scope of other numbered slices.
- Introducing non-approved architecture shifts beyond this slice.
